const app = {
  textAlign: 'center',
  backgroundColor: 'red',
  color: 'blue'
};

const appLogo = {
  height: '40vmin',
  pointerEvents: 'none'
}

const appHeader = {
  minHeight: '100vh',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  fontSize: 'calc(10px + 2vmin)',
}

const styles = { app, appLogo, appHeader};

export default styles;